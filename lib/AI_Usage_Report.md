# AI Usage Report

**1. AI Tools Used:** - Gemini (Google)

**2. What AI helped me generate:**
- **Documentation:** Interpreted the exam draft requirements and generated the structured Product Requirement Document (PRD).
- **Code Scaffolding:** Provided boilerplate Flutter code for the 3 main screens (Home, Check-in, Finish Class).
- **Logic Integration:** Generated code examples for hardware packages (`geolocator`, `mobile_scanner`) and local data storage (`shared_preferences`).
- **Troubleshooting:** Assisted in explaining and resolving Git terminal errors (`remote origin already exists`, `untracked files`).

**3. What I modified or implemented myself:**
- **Project Assembly:** Created the actual Flutter project, installed necessary dependencies in `pubspec.yaml`, and linked the UI screens together.
- **Version Control:** Manually executed Git commands to initialize the local repository, commit files, and push the source code to GitHub.
- **Deployment:** Executed the `flutter build web` command and manually configured Firebase Hosting via the Firebase CLI to deploy the web app.
- **Testing & Refinement:** Verified the navigation flow, ensured the form inputs (expectations, mood) worked correctly, and made sure the app met all exam constraints within the 3-hour limit.